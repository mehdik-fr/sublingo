# Used by `npm run build:staging` or `npm run build:production`.
# Local builds default to http://127.0.0.1:8765 and need no file.
SUBLINGO_BACKEND_BASE_URL=https://api.example.invalid
SUBLINGO_REQUEST_TIMEOUT_MS=15000
SUBLINGO_MAX_RETRIES=1
