{
  "name": "sublingo",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension for learning languages through interactive subtitles.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  }
}
